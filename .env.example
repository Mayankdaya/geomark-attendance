# Copy to .env for local dev. On Vercel, set these in
# Project Settings → Environment Variables instead.
# Use the POOLED connection string from Neon (adds ?pgbouncer=true).
DATABASE_URL="postgresql://user:password@ep-xxxx-pooler.ap-south-1.aws.neon.tech/geomark?sslmode=require&pgbouncer=true&connection_limit=1"
